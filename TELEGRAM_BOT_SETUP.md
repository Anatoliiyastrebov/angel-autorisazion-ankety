# 🤖 Настройка Telegram бота для авторизации через Web App

## 📋 Обзор

Этот проект использует **Telegram Web App** для авторизации пользователей. Пользователи открывают сайт через бота в Telegram, и авторизация происходит автоматически без необходимости ввода данных.

---

## 🚀 Пошаговая настройка

### Шаг 1: Создание бота (если еще не создан)

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: `Мой бот для анкет`)
   - Введите username бота (например: `telega_automat_bot`)
4. Сохраните **токен бота** (например: `8517653898:AAHwVsdsjErVCNoVGrHhVZUGptPWdrtOC2A`)

### Шаг 2: Настройка Web App

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/mybots`
3. Выберите вашего бота
4. Выберите **"Bot Settings"** → **"Menu Button"** или **"Commands"**
5. Настройте кнопку меню или команду для открытия Web App

#### Вариант A: Настройка через Menu Button (рекомендуется)

1. В [@BotFather](https://t.me/BotFather) выберите вашего бота
2. Выберите **"Bot Settings"** → **"Menu Button"**
3. Выберите **"Configure Menu Button"**
4. Введите:
   - **Text**: `Открыть анкеты` (или любой другой текст)
   - **URL**: `https://ваш-домен.vercel.app` (URL вашего сайта на Vercel)

#### Вариант B: Настройка через команду

1. В [@BotFather](https://t.me/BotFather) выберите вашего бота
2. Выберите **"Bot Settings"** → **"Commands"**
3. Добавьте команду:
   - **Command**: `start` или `anketa`
   - **Description**: `Открыть анкеты`

Затем в коде бота (если у вас есть бэкенд) добавьте обработчик команды, который отправляет кнопку с Web App:

```javascript
// Пример для Node.js с библиотекой node-telegram-bot-api
bot.onText(/\/start/, (msg) => {
  bot.sendMessage(msg.chat.id, 'Откройте анкеты:', {
    reply_markup: {
      inline_keyboard: [[
        {
          text: 'Открыть анкеты',
          web_app: { url: 'https://ваш-домен.vercel.app' }
        }
      ]]
    }
  })
})
```

#### Вариант C: Настройка через Inline Keyboard (в коде бота)

Если у вас есть бэкенд для бота, вы можете отправлять сообщения с кнопкой Web App:

```javascript
// Пример для Node.js
const TelegramBot = require('node-telegram-bot-api')

const bot = new TelegramBot('YOUR_BOT_TOKEN', { polling: true })

bot.onText(/\/start/, (msg) => {
  const chatId = msg.chat.id
  
  bot.sendMessage(chatId, 'Добро пожаловать! Нажмите кнопку ниже, чтобы открыть анкеты:', {
    reply_markup: {
      inline_keyboard: [[
        {
          text: '📋 Открыть анкеты',
          web_app: { url: 'https://ваш-домен.vercel.app' }
        }
      ]]
    }
  })
})
```

### Шаг 3: Получение Chat ID группы (для отправки анкет)

1. Добавьте бота [@userinfobot](https://t.me/userinfobot) в вашу группу
2. Отправьте любое сообщение в группу
3. Бот вернет `chat_id` группы (например: `-5074397630`)
4. Сохраните этот ID

### Шаг 4: Настройка переменных окружения

#### На Vercel:

1. Перейдите в [Vercel Dashboard](https://vercel.com)
2. Выберите проект
3. Перейдите в **Settings** → **Environment Variables**
4. Добавьте следующие переменные:

| Переменная | Значение | Пример |
|------------|----------|--------|
| `NEXT_PUBLIC_TELEGRAM_BOT_NAME` | Имя бота (username) | `telega_automat_bot` |
| `TELEGRAM_BOT_TOKEN` | Токен бота от BotFather | `8517653898:AAHwVsdsjErVCNoVGrHhVZUGptPWdrtOC2A` |
| `TELEGRAM_GROUP_CHAT_ID` | ID группы для анкет | `-5074397630` |
| `TELEGRAM_ADMIN_CHAT_ID` | Ваш личный chat_id (опционально) | `123456789` |

5. **Важно:** После добавления переменных пересоберите проект:
   - **Deployments** → три точки (...) → **Redeploy**

#### Локально:

Создайте файл `.env.local` в корне проекта:

```env
NEXT_PUBLIC_TELEGRAM_BOT_NAME=telega_automat_bot
TELEGRAM_BOT_TOKEN=8517653898:AAHwVsdsjErVCNoVGrHhVZUGptPWdrtOC2A
TELEGRAM_GROUP_CHAT_ID=-5074397630
TELEGRAM_ADMIN_CHAT_ID=your_chat_id_here
```

---

## ✅ Проверка работы

### 1. Проверка Web App

1. Откройте бота в Telegram
2. Нажмите на кнопку меню или отправьте команду `/start`
3. Нажмите на кнопку "Открыть анкеты"
4. Должно открыться Web App с автоматической авторизацией
5. Вы должны увидеть выбор анкет без необходимости дополнительной авторизации

### 2. Проверка отправки анкет

1. Заполните любую анкету
2. Отправьте анкету
3. Проверьте группу Telegram - анкета должна быть отправлена туда

---

## 🔧 Устранение проблем

### Проблема: Web App не открывается

**Решение:**
- Убедитесь, что URL в настройках бота правильный (https://)
- Проверьте, что сайт доступен по этому URL
- Убедитесь, что домен не заблокирован Telegram

### Проблема: Авторизация не происходит автоматически

**Решение:**
- Проверьте, что скрипт `telegram-web-app.js` загружается (откройте DevTools → Network)
- Убедитесь, что сайт открыт через Telegram (не в обычном браузере)
- Проверьте консоль браузера на наличие ошибок

### Проблема: Данные пользователя не сохраняются

**Решение:**
- Проверьте, что `localStorage` доступен в браузере
- Убедитесь, что сайт открыт через HTTPS (обязательно для Web App)
- Проверьте консоль на наличие ошибок JavaScript

### Проблема: Анкеты не отправляются в группу

**Решение:**
- Проверьте, что бот добавлен в группу как администратор
- Убедитесь, что `TELEGRAM_BOT_TOKEN` правильный
- Проверьте, что `TELEGRAM_GROUP_CHAT_ID` правильный
- Проверьте логи на Vercel (Functions → Logs)

---

## 📱 Примеры использования

### Пример 1: Простая кнопка меню

```
Бот → Меню → "Открыть анкеты" → Web App открывается
```

### Пример 2: Команда /start

```
Пользователь: /start
Бот: [Кнопка "Открыть анкеты"]
Пользователь: [Нажимает кнопку] → Web App открывается
```

### Пример 3: Inline кнопка в сообщении

```
Бот отправляет сообщение с кнопкой Web App
Пользователь нажимает кнопку → Web App открывается
```

---

## 🔐 Безопасность

1. **Никогда не публикуйте токен бота** в публичных репозиториях
2. **Используйте HTTPS** для Web App (обязательно)
3. **Проверяйте подпись данных** на сервере (уже реализовано в `app/api/submit/route.ts`)
4. **Ограничьте доступ** к админским функциям через `TELEGRAM_ADMIN_CHAT_ID`

---

## 📚 Дополнительные ресурсы

- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [Telegram Web App Documentation](https://core.telegram.org/bots/webapps)
- [BotFather Commands](https://core.telegram.org/bots#6-botfather)

---

## ✅ Чеклист настройки

- [ ] Бот создан через @BotFather
- [ ] Токен бота сохранен
- [ ] Menu Button или команда настроена в боте
- [ ] URL сайта добавлен в настройки бота
- [ ] Переменные окружения настроены на Vercel
- [ ] Бот добавлен в группу как администратор
- [ ] Chat ID группы получен и сохранен
- [ ] Web App открывается через бота
- [ ] Авторизация происходит автоматически
- [ ] Анкеты отправляются в группу

---

**Готово!** Теперь ваш бот готов к работе с авторизацией через Telegram Web App. 🎉

