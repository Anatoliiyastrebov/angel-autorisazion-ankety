# Переменные окружения проекта

## 📋 Полный список переменных окружения

### 🔵 Next.js (текущий проект)

#### Клиентские переменные (NEXT_PUBLIC_*)
Доступны на клиенте и встраиваются в код во время сборки.

| Переменная | Описание | Обязательная | Где используется |
|------------|----------|--------------|------------------|
| `NEXT_PUBLIC_TELEGRAM_BOT_NAME` | Имя бота для Telegram Login Widget (например: `telega_automat_bot`) | ✅ Да | `components/QuestionnaireForm.tsx`, `components/TelegramLogin.tsx` |

#### Серверные переменные
Доступны только на сервере (в API routes).

| Переменная | Описание | Обязательная | Где используется | Значение по умолчанию |
|------------|----------|--------------|------------------|----------------------|
| `TELEGRAM_BOT_TOKEN` | Токен бота Telegram (получить у @BotFather) | ✅ Да | `app/api/submit/route.ts` | - |
| `TELEGRAM_GROUP_CHAT_ID` | ID группы/супергруппы для отправки анкет | ⚠️ Опционально | `app/api/submit/route.ts` | `-1003533385546` |
| `TELEGRAM_ADMIN_CHAT_ID` | ID администратора для уведомлений | ⚠️ Опционально | `app/api/submit/route.ts` | - |

---

### 🟢 Vite (старая часть проекта)

Эти переменные используются в старых компонентах проекта (`src/`), которые могут быть неактивны.

| Переменная | Описание | Обязательная | Где используется |
|------------|----------|--------------|------------------|
| `VITE_TELEGRAM_BOT_TOKEN` | Токен бота для старой части проекта | ⚠️ Опционально | `src/pages/Anketa.tsx`, `src/lib/form-utils.ts`, `api/auth/send-otp.ts` |
| `VITE_TELEGRAM_CHAT_ID` | Chat ID для старой части проекта | ⚠️ Опционально | `src/pages/Anketa.tsx`, `src/lib/form-utils.ts` |

---

### 🟡 Supabase (для хранения данных)

| Переменная | Описание | Обязательная | Где используется |
|------------|----------|--------------|------------------|
| `SUPABASE_URL` | URL проекта Supabase | ⚠️ Опционально | `api/questionnaires/save.ts`, `api/questionnaires/get.ts`, `api/questionnaires/delete.ts`, `api/auth/send-otp.ts`, `api/auth/verify-otp.ts`, `api/gdpr/create-request.ts`, `supabase/functions/delete_old_profiles/index.ts` |
| `SUPABASE_SERVICE_ROLE_KEY` | Service Role Key из Supabase | ⚠️ Опционально | Те же файлы, что и `SUPABASE_URL` |

---

### 🔴 Шифрование

| Переменная | Описание | Обязательная | Где используется |
|------------|----------|--------------|------------------|
| `ENCRYPTION_KEY` | Ключ шифрования (32 байта hex) | ⚠️ Опционально | `api/questionnaires/save.ts`, `api/questionnaires/get.ts` |

---

## 📝 Пример файла `.env.local`

Создайте файл `.env.local` в корне проекта:

```env
# ============================================
# TELEGRAM CONFIGURATION (Next.js)
# ============================================

# Имя бота для Telegram Login Widget
# Получить у @BotFather в Telegram
NEXT_PUBLIC_TELEGRAM_BOT_NAME=your_bot_name_here

# Токен бота для отправки сообщений
# Получить у @BotFather в Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here

# ID группы/супергруппы для отправки анкет
# Получить можно через бота @userinfobot или из URL группы
TELEGRAM_GROUP_CHAT_ID=-1003533385546

# ID администратора для уведомлений (опционально)
# Получить у @userinfobot
TELEGRAM_ADMIN_CHAT_ID=your_chat_id_here

# ============================================
# SUPABASE CONFIGURATION (опционально)
# ============================================

# URL проекта Supabase
SUPABASE_URL=https://your-project.supabase.co

# Service Role Key из Supabase
# ⚠️ ВАЖНО: Никогда не публикуйте этот ключ!
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ============================================
# ENCRYPTION (опционально)
# ============================================

# Ключ шифрования (32 байта hex)
# Сгенерировать: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your_32_byte_hex_key_here

# ============================================
# VITE CONFIGURATION (старая часть проекта)
# ============================================

# Токен бота для старой части (если используется)
VITE_TELEGRAM_BOT_TOKEN=your_bot_token_here

# Chat ID для старой части (если используется)
VITE_TELEGRAM_CHAT_ID=your_chat_id_here
```

---

## 🚀 Настройка на Vercel

### Обязательные переменные для работы анкет:

1. **`NEXT_PUBLIC_TELEGRAM_BOT_NAME`** - имя бота
2. **`TELEGRAM_BOT_TOKEN`** - токен бота
3. **`TELEGRAM_GROUP_CHAT_ID`** - ID группы (или будет использован по умолчанию)

### Опциональные переменные:

- `TELEGRAM_ADMIN_CHAT_ID` - для уведомлений администратору
- `SUPABASE_URL` - если используется Supabase
- `SUPABASE_SERVICE_ROLE_KEY` - если используется Supabase
- `ENCRYPTION_KEY` - если используется шифрование

---

## 📍 Где получить значения

### Telegram Bot Token и Bot Name:
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` или `/mybots`
3. Скопируйте токен и имя бота

### Telegram Chat ID:
1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте любое сообщение
3. Бот вернет ваш `chat_id`

### Telegram Group Chat ID:
1. Добавьте бота [@userinfobot](https://t.me/userinfobot) в группу
2. Отправьте любое сообщение в группу
3. Бот вернет `chat_id` группы

### Supabase:
1. Перейдите в [Supabase Dashboard](https://app.supabase.com)
2. Выберите проект
3. Settings → API → скопируйте `URL` и `service_role key`

### Encryption Key:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

---

## ⚠️ Важные замечания

1. **Никогда не коммитьте `.env.local` в репозиторий!**
2. **`NEXT_PUBLIC_*` переменные встраиваются в клиентский код** - не храните там секреты!
3. **`TELEGRAM_BOT_TOKEN` и `SUPABASE_SERVICE_ROLE_KEY`** - это секреты, храните их только на сервере
4. **После добавления переменных на Vercel** - обязательно пересоберите проект!

---

## 🔍 Проверка переменных

После настройки переменных проверьте:

1. **Локально**: создайте `.env.local` и запустите `npm run dev`
2. **На Vercel**: добавьте переменные в Project Settings → Environment Variables
3. **Пересоберите проект** на Vercel после добавления переменных

