# 🔘 Настройка встроенной кнопки Menu Button в боте

## 📋 Описание

Вместо отправки кнопки сообщением, используется **встроенная кнопка Menu Button** в боте. При нажатии на неё открывается Web App с подтверждением авторизации.

---

## 🚀 Настройка через BotFather

### Шаг 1: Откройте BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/mybots`
3. Выберите вашего бота `@telega_automat_bot`

### Шаг 2: Настройте Menu Button

1. Выберите **"Bot Settings"**
2. Выберите **"Menu Button"**
3. Выберите **"Configure Menu Button"**
4. Введите:
   - **Text**: `Авторизоваться` (или любой другой текст)
   - **URL**: `https://autorisazion-telegram.vercel.app/auth/confirm`

### Шаг 3: Готово!

Теперь внизу экрана бота появится кнопка "Авторизоваться". При нажатии на неё откроется Web App с подтверждением авторизации.

---

## ✅ Как это работает

1. Пользователь открывает бота `@telega_automat_bot`
2. Внизу экрана видит встроенную кнопку **"Авторизоваться"**
3. Нажимает на кнопку
4. Открывается Web App с диалогом подтверждения авторизации
5. Пользователь видит свои данные из Telegram
6. Нажимает "Подтвердить авторизацию"
7. Происходит возврат на сайт с автоматической авторизацией
8. Пользователь видит выбор анкет

---

## 🔧 Альтернатива: Настройка через Bot API

Если вы хотите настроить Menu Button программно, используйте Bot API:

```javascript
const TelegramBot = require('node-telegram-bot-api')

const bot = new TelegramBot(process.env.TELEGRAM_BOT_TOKEN, { polling: true })

// Установка Menu Button
bot.setChatMenuButton({
  menu_button: {
    type: 'web_app',
    text: 'Авторизоваться',
    web_app: {
      url: 'https://autorisazion-telegram.vercel.app/auth/confirm'
    }
  }
}).then(() => {
  console.log('✅ Menu Button настроен')
}).catch((error) => {
  console.error('❌ Ошибка настройки Menu Button:', error)
})
```

Добавьте этот код в `bot/index.js` после инициализации бота, если хотите настроить Menu Button автоматически.

---

## 📱 Что видит пользователь

### В боте:
```
[Встроенная кнопка "Авторизоваться" внизу экрана]
```

### В Web App (после нажатия):
```
🔐 Подтверждение авторизации

Вы хотите авторизоваться на сайте с данными:

Имя: Иван Иванов
Telegram: @username
ID: 123456789

[✅ Подтвердить авторизацию]
[Отмена]
```

---

## ✅ Преимущества Menu Button

- ✅ Всегда видна внизу экрана
- ✅ Не занимает место в чате
- ✅ Более удобный UX
- ✅ Не нужно отправлять сообщения

---

## 🔍 Устранение проблем

### Проблема: Кнопка не появляется

**Решение:**
1. Убедитесь, что Menu Button настроена через BotFather
2. Проверьте, что URL правильный и доступен по HTTPS
3. Перезапустите Telegram (закройте и откройте приложение)
4. Проверьте, что бот не заблокирован

### Проблема: Web App не открывается

**Решение:**
1. Проверьте, что URL правильный: `https://autorisazion-telegram.vercel.app/auth/confirm`
2. Убедитесь, что сайт доступен по HTTPS
3. Проверьте, что домен не заблокирован Telegram

### Проблема: Данные не отображаются в Web App

**Решение:**
1. Убедитесь, что открыто через Menu Button (не через обычную ссылку)
2. Проверьте консоль браузера на наличие ошибок
3. Убедитесь, что скрипт `telegram-web-app.js` загружается

---

## 📚 Дополнительная информация

- [Telegram Bot API - Menu Button](https://core.telegram.org/bots/api#menubutton)
- [Telegram Web App API](https://core.telegram.org/bots/webapps)

---

**Готово!** Теперь используется встроенная кнопка Menu Button вместо отправки кнопки сообщением. 🎉

