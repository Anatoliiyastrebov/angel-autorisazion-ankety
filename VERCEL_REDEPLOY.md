# 🔄 Инструкция по пересборке проекта на Vercel

## Проблема
Если вы видите старое сообщение об ошибке переменных окружения VITE_TELEGRAM_BOT_TOKEN, значит на Vercel используется старый деплой.

## Решение: Принудительная пересборка

### Вариант 1: Через Vercel Dashboard (Рекомендуется)

1. **Откройте Vercel Dashboard:**
   - Перейдите на https://vercel.com
   - Войдите в аккаунт
   - Выберите проект `autorisazion-telegram`

2. **Перейдите в Deployments:**
   - В меню слева выберите **"Deployments"**
   - Найдите последний деплой

3. **Принудительная пересборка:**
   - Нажмите на **три точки (...)** рядом с последним деплоем
   - Выберите **"Redeploy"**
   - В появившемся окне выберите **"Use existing Build Cache"** = **OFF** (отключить кэш)
   - Нажмите **"Redeploy"**

4. **Дождитесь завершения:**
   - Деплой займет 2-5 минут
   - После завершения откройте ваш сайт

### Вариант 2: Через Git (Автоматический деплой)

1. **Создайте пустой коммит:**
   ```bash
   git commit --allow-empty -m "Force redeploy on Vercel"
   git push origin main
   ```

2. **Vercel автоматически пересоберет проект**

### Вариант 3: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в Vercel
vercel login

# Пересоберите проект
vercel --prod --force
```

## ✅ Проверка после пересборки

1. **Откройте ваш сайт на Vercel**
2. **Очистите кэш браузера:**
   - `Ctrl+Shift+R` (Windows) или `Cmd+Shift+R` (Mac)
   - Или откройте в режиме инкогнито

3. **Проверьте, что:**
   - ✅ Нет сообщения об ошибке переменных окружения
   - ✅ Показывается форма анкеты
   - ✅ Авторизация в конце анкеты

## 🔍 Если проблема сохраняется

1. **Проверьте переменные окружения в Vercel:**
   - Settings → Environment Variables
   - Убедитесь, что есть:
     - `NEXT_PUBLIC_TELEGRAM_BOT_NAME` = `telega_automat_bot`
     - `TELEGRAM_BOT_TOKEN` = `8517653898:AAHwVsdsjErVCNoVGrHhVZUGptPWdrtOC2A`
     - `TELEGRAM_GROUP_CHAT_ID` = `-5074397630` (опционально)

2. **Проверьте логи деплоя:**
   - В Vercel Dashboard → Deployments → выберите деплой → Logs
   - Убедитесь, что сборка прошла успешно

3. **Проверьте, что используется правильный фреймворк:**
   - Settings → General
   - Framework Preset должен быть **Next.js**

## 📝 Важные замечания

- После изменения `vercel.json` обязательно пересоберите проект
- Переменные окружения применяются только после пересборки
- Кэш браузера может показывать старую версию - очистите его

