# 🔘 Настройка кнопки "Авторизоваться" в боте

## ✅ Что уже сделано

Кнопка "Авторизоваться" в боте теперь открывает **Web App** с диалогом подтверждения авторизации через Telegram.

---

## 📋 Как это работает

1. Пользователь нажимает "Войти через Telegram" на сайте
2. Открывается бот в Telegram
3. Бот отправляет сообщение с кнопкой **"✅ Авторизоваться"**
4. При нажатии на кнопку открывается **Web App** с диалогом подтверждения
5. Пользователь видит свои данные и нажимает "Подтвердить авторизацию"
6. Происходит возврат на сайт с автоматической авторизацией

---

## 🤖 Код для бота

Используйте код из файла `bot-example.js`. Ключевая часть:

```javascript
// Отправляем сообщение с кнопкой Web App
bot.sendMessage(chatId, '🔐 Для авторизации на сайте нажмите кнопку ниже:', {
  reply_markup: {
    inline_keyboard: [[
      {
        text: '✅ Авторизоваться',
        web_app: { 
          url: `${SITE_URL}/auth/confirm?session=${sessionId}&token=${token}&user_id=${chatId}` 
        }
      }
    ]]
  }
})
```

**Важно:** Используйте `web_app` вместо `url` для открытия Web App внутри Telegram.

---

## 🔧 Настройка

### 1. Установите зависимости

```bash
npm install node-telegram-bot-api axios
```

### 2. Настройте переменные окружения

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
SITE_URL=https://ваш-домен.vercel.app
```

### 3. Запустите бота

```bash
node bot-example.js
```

---

## 📱 Что видит пользователь

### В боте:
```
🔐 Для авторизации на сайте нажмите кнопку ниже:

[✅ Авторизоваться]  ← Кнопка Web App
```

### В Web App (после нажатия):
```
🔐 Подтверждение авторизации

Вы хотите авторизоваться на сайте с данными:

Имя: Иван Иванов
Telegram: @username
ID: 123456789

[✅ Подтвердить авторизацию]
[Отмена]
```

---

## ✅ Проверка

1. Откройте сайт
2. Нажмите "Войти через Telegram"
3. В боте должна появиться кнопка "✅ Авторизоваться"
4. Нажмите на кнопку - должен открыться Web App
5. В Web App должны отображаться ваши данные
6. Нажмите "Подтвердить авторизацию"
7. Должен произойти возврат на сайт с авторизацией

---

## 🔍 Устранение проблем

### Проблема: Web App не открывается

**Решение:**
- Убедитесь, что используете `web_app` вместо `url` в кнопке
- Проверьте, что URL правильный и доступен по HTTPS
- Убедитесь, что домен добавлен в настройки бота в BotFather

### Проблема: Данные не отображаются в Web App

**Решение:**
- Проверьте, что токен передается в URL
- Проверьте логи бота на наличие ошибок
- Проверьте консоль браузера в Web App (если доступна)

### Проблема: Авторизация не происходит после подтверждения

**Решение:**
- Проверьте, что данные сохраняются в localStorage
- Проверьте консоль браузера на наличие ошибок
- Убедитесь, что токен не истек (действителен 5 минут)

---

## 📚 Дополнительная информация

- [Telegram Bot API - Web Apps](https://core.telegram.org/bots/webapps)
- [Telegram Web App API](https://core.telegram.org/bots/webapps#initializing-mini-apps)

---

**Готово!** Теперь кнопка "Авторизоваться" открывает Web App с диалогом подтверждения. 🎉

