# 🤖 Telegram бот для авторизации

Бот `@telega_automat_bot` для авторизации пользователей на сайте через Telegram Web App.

## 🚀 Быстрый старт

### 1. Установите зависимости

```bash
cd bot
npm install
```

### 2. Настройте переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите:
- `TELEGRAM_BOT_TOKEN` - токен вашего бота от @BotFather
- `SITE_URL` - URL вашего сайта на Vercel (например: `https://your-site.vercel.app`)

### 3. Запустите бота

```bash
npm start
```

Или для разработки:

```bash
npm run dev
```

## 📋 Переменные окружения

| Переменная | Описание | Пример |
|------------|----------|--------|
| `TELEGRAM_BOT_TOKEN` | Токен бота от @BotFather | `8517653898:AAHwVsdsjErVCNoVGrHhVZUGptPWdrtOC2A` |
| `SITE_URL` | URL вашего сайта | `https://your-site.vercel.app` |

## 🔧 Как это работает

1. Пользователь нажимает "Войти через Telegram" на сайте
2. Сайт открывает бота с командой `/start auth_<sessionId>`
3. Бот получает токен авторизации с сайта через API
4. Бот отправляет сообщение с кнопкой "✅ Авторизоваться" (Web App)
5. При нажатии открывается Web App с диалогом подтверждения
6. После подтверждения пользователь возвращается на сайт авторизованным

## 📝 Команды бота

- `/start` - Начать работу с ботом
- `/start auth_<sessionId>` - Авторизация (вызывается автоматически с сайта)
- `/help` - Показать справку

## 🐛 Устранение проблем

### Бот не отвечает

1. Проверьте, что токен бота правильный
2. Убедитесь, что бот запущен (`npm start`)
3. Проверьте логи на наличие ошибок

### Ошибка "API сайта недоступен"

1. Проверьте, что `SITE_URL` правильный
2. Убедитесь, что сайт развернут на Vercel
3. Проверьте, что API endpoint `/api/auth/generate-token` доступен

### Ошибка "Неверный токен бота"

1. Проверьте `TELEGRAM_BOT_TOKEN` в `.env`
2. Убедитесь, что токен совпадает с токеном на сайте (Vercel)
3. Перезапустите бота после изменения токена

## 📦 Зависимости

- `node-telegram-bot-api` - библиотека для работы с Telegram Bot API
- `axios` - HTTP клиент для запросов к API сайта
- `dotenv` - загрузка переменных окружения из `.env`

## 🚀 Деплой

### Локально

Просто запустите:
```bash
npm start
```

### На сервере (PM2)

```bash
npm install -g pm2
pm2 start index.js --name telegram-auth-bot
pm2 save
pm2 startup
```

### На Heroku

1. Создайте `Procfile`:
```
worker: node index.js
```

2. Установите переменные окружения в Heroku Dashboard

3. Деплойте:
```bash
git push heroku main
```

### На Railway/Render

1. Подключите репозиторий
2. Укажите команду запуска: `node index.js`
3. Добавьте переменные окружения в настройках

## 📚 Дополнительная информация

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [node-telegram-bot-api](https://github.com/yagop/node-telegram-bot-api)

---

**Готово!** Бот готов к работе. 🎉

