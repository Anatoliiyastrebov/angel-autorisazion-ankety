# ✅ Полная настройка бота - Инструкция

## 📋 Что уже готово

✅ Полный рабочий код бота в папке `bot/`  
✅ Все API endpoints настроены  
✅ Web App для подтверждения авторизации  
✅ Инструкции и документация  

---

## 🚀 Шаг 1: Настройка сайта на Vercel

### 1.1. Добавьте переменные окружения в Vercel

Перейдите в **Vercel Dashboard** → ваш проект → **Settings** → **Environment Variables**

Добавьте следующие переменные:

| Переменная | Значение | Пример |
|------------|----------|--------|
| `NEXT_PUBLIC_TELEGRAM_BOT_NAME` | `telega_automat_bot` | `telega_automat_bot` |
| `TELEGRAM_BOT_TOKEN` | Токен вашего бота | `8517653898:AAHwVsdsjErVCNoVGrHhVZUGptPWdrtOC2A` |
| `TELEGRAM_GROUP_CHAT_ID` | ID группы | `-5074397630` |
| `NEXT_PUBLIC_SITE_URL` | URL вашего сайта | `https://your-site.vercel.app` |

**Важно:** После добавления переменных **пересоберите проект**:
- **Deployments** → три точки (...) → **Redeploy**

---

## 🤖 Шаг 2: Настройка и запуск бота

### 2.1. Перейдите в папку бота

```bash
cd bot
```

### 2.2. Установите зависимости

```bash
npm install
```

### 2.3. Создайте файл .env

Создайте файл `.env` в папке `bot/`:

```env
TELEGRAM_BOT_TOKEN=8517653898:AAHwVsdsjErVCNoVGrHhVZUGptPWdrtOC2A
SITE_URL=https://ваш-домен.vercel.app
```

**Замените:**
- `TELEGRAM_BOT_TOKEN` - на токен вашего бота от @BotFather
- `SITE_URL` - на реальный URL вашего сайта на Vercel

### 2.4. Запустите бота

```bash
npm start
```

Вы должны увидеть:
```
🤖 Telegram бот запускается...
📡 SITE_URL: https://your-site.vercel.app
🔑 Бот токен: 8517653898...
✅ API сайта доступен
✅ Бот запущен и готов к работе!
```

---

## ✅ Шаг 3: Проверка работы

### 3.1. Проверьте бота

1. Откройте Telegram
2. Найдите бота `@telega_automat_bot`
3. Отправьте команду `/start`
4. Бот должен ответить приветствием

### 3.2. Проверьте авторизацию

1. Откройте ваш сайт на Vercel
2. Нажмите кнопку "Войти через Telegram"
3. Должен открыться бот в Telegram
4. Бот должен отправить сообщение с кнопкой "✅ Авторизоваться"
5. Нажмите на кнопку - должен открыться Web App
6. В Web App нажмите "Подтвердить авторизацию"
7. Должен произойти возврат на сайт с авторизацией

---

## 🐛 Устранение проблем

### Проблема: Бот не отвечает

**Решение:**
1. Проверьте, что бот запущен (`npm start` в папке `bot/`)
2. Проверьте токен бота в `.env`
3. Проверьте логи на наличие ошибок

### Проблема: "API сайта недоступен"

**Решение:**
1. Проверьте `SITE_URL` в `.env` бота
2. Убедитесь, что URL правильный (с `https://`)
3. Проверьте, что сайт развернут на Vercel
4. Проверьте, что переменные окружения настроены на Vercel

### Проблема: "Неверный токен бота" (401)

**Решение:**
1. Убедитесь, что `TELEGRAM_BOT_TOKEN` одинаковый:
   - В `.env` бота
   - В переменных окружения Vercel
2. Перезапустите бота после изменения токена
3. Пересоберите проект на Vercel

### Проблема: Web App не открывается

**Решение:**
1. Убедитесь, что используете `web_app` в кнопке (уже настроено)
2. Проверьте, что URL правильный и доступен по HTTPS
3. Убедитесь, что домен добавлен в настройки бота в BotFather (если требуется)

---

## 📦 Деплой бота на сервер

### Вариант 1: PM2 (рекомендуется)

```bash
# Установите PM2
npm install -g pm2

# Запустите бота
cd bot
pm2 start index.js --name telegram-auth-bot

# Сохраните конфигурацию
pm2 save

# Настройте автозапуск
pm2 startup
```

### Вариант 2: systemd (Linux)

Создайте файл `/etc/systemd/system/telegram-bot.service`:

```ini
[Unit]
Description=Telegram Auth Bot
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/bot
Environment="NODE_ENV=production"
ExecStart=/usr/bin/node index.js
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
```

### Вариант 3: Docker

Создайте `Dockerfile` в папке `bot/`:

```dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install --production

COPY . .

CMD ["node", "index.js"]
```

Запуск:
```bash
docker build -t telegram-bot .
docker run -d --name telegram-bot --env-file .env telegram-bot
```

---

## 📝 Чеклист настройки

- [ ] Переменные окружения добавлены в Vercel
- [ ] Проект пересобран на Vercel
- [ ] Зависимости бота установлены (`npm install` в папке `bot/`)
- [ ] Файл `.env` создан в папке `bot/`
- [ ] Токен бота указан в `.env`
- [ ] URL сайта указан в `.env`
- [ ] Бот запущен (`npm start`)
- [ ] Бот отвечает на команду `/start`
- [ ] Авторизация работает через сайт

---

## 🎉 Готово!

Теперь все настроено и готово к работе:

1. ✅ Сайт развернут на Vercel
2. ✅ Бот настроен и запущен
3. ✅ Авторизация работает через Web App

**Следующие шаги:**
- Настройте автозапуск бота на сервере (PM2, systemd, Docker)
- Настройте мониторинг и логирование
- При необходимости добавьте дополнительные функции

---

**Вопросы?** Проверьте:
- `bot/README.md` - документация бота
- `BOT_BUTTON_SETUP.md` - настройка кнопки
- `TELEGRAM_BOT_SETUP.md` - общая настройка

