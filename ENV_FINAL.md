# ✅ Финальный список переменных окружения

## 📋 Используемые переменные

Проект использует **только** следующие переменные окружения:

### 1. NEXT_PUBLIC_TELEGRAM_BOT_NAME ✅
- **Тип:** Клиентская (доступна в браузере)
- **Обязательная:** Да
- **Значение:** Имя вашего Telegram бота (например: `telega_automat_bot`)
- **Где используется:** 
  - `components/QuestionnaireForm.tsx`
  - `components/TelegramLogin.tsx`
- **Где получить:** @BotFather в Telegram → `/mybots` → выберите бота → скопируйте имя

### 2. TELEGRAM_BOT_TOKEN ✅
- **Тип:** Серверная (только на сервере)
- **Обязательная:** Да
- **Значение:** Токен вашего Telegram бота
- **Где используется:**
  - `app/api/submit/route.ts` - для отправки сообщений
  - `api/auth/send-otp.ts` - для отправки OTP
- **Где получить:** @BotFather в Telegram → `/mybots` → выберите бота → API Token

### 3. TELEGRAM_GROUP_CHAT_ID ✅
- **Тип:** Серверная
- **Обязательная:** Нет (есть значение по умолчанию: `-5074397630`)
- **Значение:** ID группы/супергруппы для отправки анкет
- **Где используется:**
  - `app/api/submit/route.ts`
- **Где получить:** Добавьте @userinfobot в группу → отправьте сообщение → бот вернет chat_id

### 4. TELEGRAM_ADMIN_CHAT_ID ✅
- **Тип:** Серверная
- **Обязательная:** Нет
- **Значение:** ID администратора для уведомлений
- **Где используется:**
  - `app/api/submit/route.ts` - для отправки уведомлений администратору
- **Где получить:** @userinfobot в Telegram → отправьте сообщение → бот вернет ваш chat_id

---

## ❌ Удаленные переменные (не используются)

Следующие переменные **удалены** и больше не используются:
- ❌ `VITE_TELEGRAM_BOT_TOKEN` - заменена на `TELEGRAM_BOT_TOKEN`
- ❌ `VITE_TELEGRAM_CHAT_ID` - заменена на `TELEGRAM_GROUP_CHAT_ID`
- ❌ `VITE_API_BASE_URL` - не используется
- ❌ `SUPABASE_URL` - не используется
- ❌ `SUPABASE_SERVICE_ROLE_KEY` - не используется
- ❌ `SUPABASE_ANON_KEY` - не используется
- ❌ `ENCRYPTION_KEY` - не используется (опционально, если нужно шифрование)

---

## 📝 Пример .env.local

```env
# Обязательные переменные
NEXT_PUBLIC_TELEGRAM_BOT_NAME=telega_automat_bot
TELEGRAM_BOT_TOKEN=8517653898:AAHwVsdsjErVCNoVGrHhVZUGptPWdrtOC2A

# Опциональные переменные
TELEGRAM_GROUP_CHAT_ID=-5074397630
TELEGRAM_ADMIN_CHAT_ID=your_chat_id_here
```

---

## 🚀 Настройка на Vercel

1. Перейдите в **Vercel Dashboard** → ваш проект → **Settings** → **Environment Variables**

2. Добавьте следующие переменные:

   | Переменная | Значение | Окружения |
   |------------|----------|-----------|
   | `NEXT_PUBLIC_TELEGRAM_BOT_NAME` | `telega_automat_bot` | Production, Preview, Development |
   | `TELEGRAM_BOT_TOKEN` | `8517653898:AAHwVsdsjErVCNoVGrHhVZUGptPWdrtOC2A` | Production, Preview, Development |
   | `TELEGRAM_GROUP_CHAT_ID` | `-5074397630` | Production, Preview, Development (опционально) |
   | `TELEGRAM_ADMIN_CHAT_ID` | ваш chat_id | Production, Preview, Development (опционально) |

3. **После добавления переменных обязательно пересоберите проект:**
   - Deployments → три точки (...) → Redeploy
   - Или сделайте новый коммит в GitHub

---

## ✅ Проверка

После настройки проверьте:
- ✅ Нет сообщений об ошибке переменных окружения
- ✅ Форма анкеты отображается корректно
- ✅ Авторизация через Telegram работает
- ✅ Отправка анкет работает

